==================================================================================================
수정할 내용을 수정해서 쓰면 됩니당
==================================================================================================

첫번째 단계의 최종 목표:
- AI가 백터DB에서 프롬프트를 기반으로 유저가 질문한 내용을 정확하게 전달하는 것!
- 메모리 기능. 즉 유저가 전에 질문했던 것과 AI가 답했던 내용 전부를 요약해서 리스트에 저장해두고 다음 질문이 들어올 때 사용할 수 있게 하는 것!
- 현재는 위 기능을 터미널로 진행하여 완벽하게 해내는 것!(백엔드 연결 X, ui X)

이번 목표: openai와 터미널로 대화할 수 있도록 만들기.

기술 스택:
frontend: React tailwindcss WebSocket Axios
backend: spring boot Maven WebSocket
chatbot: FastAPI LangChain OpenAI FAISS

규칙:
- 이번 프롬프트에서 진행되는 내용과 무관한 코드의 추가 및 수정을 엄격히 금지한다.
- 

가장 기초적인 llm을 만들 예정입니다. 우선 .env 파일에 OPENAI_API_KEY를 입력하고 MODEL을 저장해둡니다.
그 후에 api_key와 해당 모델을 가져와서 메모리 기능이 없는 챗봇을 만들 것입니다.


==================================================================================================
챗봇 단계 참고
==================================================================================================

1단계 FastAPI 서버 생성
설치
pip install fastapi
pip install uvicorn

또는

pip install "fastapi[standard]"
역할

FastAPI

POST /chat
POST /rebuild-vector
GET /health

API 제공

2단계 OpenAI 연결
설치
pip install openai
역할
FastAPI
 ↓
OpenAI
 ↓
GPT
3단계 LangChain 설치
설치
pip install langchain
pip install langchain-openai
역할
Prompt
Retriever
RAG Chain
Memory

구축

4단계 문서 로더

문서를 읽기 위한 라이브러리

Markdown

기본 제공이라 설치 불필요

PDF 사용 예정이라면
pip install pypdf
DOCX 사용 예정이라면
pip install python-docx
5단계 Chunking
설치

이미 LangChain에 포함

from langchain.text_splitter import RecursiveCharacterTextSplitter

추가 설치 없음

6단계 Embedding 생성
설치
pip install langchain-openai

사용 예

from langchain_openai import OpenAIEmbeddings
7단계 FAISS 구축
설치

Windows:

pip install faiss-cpu

Linux:

pip install faiss-cpu

Mac:

pip install faiss-cpu
역할
문서
 ↓
Embedding
 ↓
Vector
 ↓
FAISS 저장
8단계 Retriever

추가 설치 없음

LangChain 포함

예:

retriever = vectorstore.as_retriever()
9단계 RAG Chain

LangChain 포함

추가 설치 없음

10단계 채팅 메모리

추천:

pip install langchain

이미 설치됨

예:

ConversationBufferWindowMemory

최근 20개 대화 기억

11단계 Spring Boot 연동

현재 Spring Boot 프로젝트에 추가

Maven
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

이미 있을 가능성 높음

FastAPI 호출

Spring에서

RestTemplate

또는

WebClient

사용

추천:

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-webflux</artifactId>
</dependency>
12단계 React 연결

이미 설치되어 있을 가능성 높음

Axios
npm install axios
13단계 스트리밍 응답
SSE 추천

추가 설치 없음

브라우저 기본 지원

EventSource

사용 가능

WebSocket 사용 시
npm install sockjs-client
npm install stompjs

하지만 챗봇은 SSE가 더 단순

14단계 사용량 제한
FastAPI

설치

pip install slowapi

예:

분당 10회

제한

15단계 환경 변수
설치
pip install python-dotenv

